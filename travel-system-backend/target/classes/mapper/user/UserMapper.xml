<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smarttravel.user.mapper.UserMapper">

    <resultMap id="UserResult" type="com.smarttravel.user.domain.User">
        <id column="id" property="id"/>
        <result column="openid" property="openid"/>
        <result column="nickname" property="nickname"/>
        <result column="avatar" property="avatar"/>
        <result column="gender" property="gender"/>
        <result column="city" property="city"/>
        <result column="signature" property="signature"/>
        <result column="email" property="email"/>
        <result column="status" property="status"/>
        <result column="note_count" property="noteCount"/>
        <result column="favorite_count" property="favoriteCount"/>
        <result column="checkin_count" property="checkinCount"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="del_flag" property="delFlag"/>
    </resultMap>

    <sql id="Base_Columns">
        id, openid, nickname, avatar, gender, city, signature, email, status,
        note_count, favorite_count, checkin_count, create_time, update_time, del_flag
    </sql>

    <select id="selectList" parameterType="com.smarttravel.user.domain.User" resultMap="UserResult">
        SELECT <include refid="Base_Columns"/> FROM user
        WHERE del_flag = 0
        <if test="nickname != null and nickname != ''">
            AND nickname LIKE CONCAT('%', #{nickname}, '%')
        </if>
        <if test="city != null and city != ''">
            AND city LIKE CONCAT('%', #{city}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="selectListWithPage" resultMap="UserResult">
        SELECT <include refid="Base_Columns"/> FROM user
        WHERE del_flag = 0
        <if test="query.nickname != null and query.nickname != ''">
            AND nickname LIKE CONCAT('%', #{query.nickname}, '%')
        </if>
        <if test="query.city != null and query.city != ''">
            AND city LIKE CONCAT('%', #{query.city}, '%')
        </if>
        <if test="query.status != null">
            AND status = #{query.status}
        </if>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countList" parameterType="com.smarttravel.user.domain.User" resultType="int">
        SELECT COUNT(*) FROM user
        WHERE del_flag = 0
        <if test="nickname != null and nickname != ''">
            AND nickname LIKE CONCAT('%', #{nickname}, '%')
        </if>
        <if test="city != null and city != ''">
            AND city LIKE CONCAT('%', #{city}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

    <select id="selectById" parameterType="long" resultMap="UserResult">
        SELECT <include refid="Base_Columns"/> FROM user
        WHERE id = #{id} AND del_flag = 0
    </select>

    <update id="updateStatus">
        UPDATE user
        SET status = #{status}, update_time = NOW()
        WHERE id = #{id} AND del_flag = 0
    </update>

    <update id="updateProfile" parameterType="com.smarttravel.user.domain.User">
        UPDATE user
        SET
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="city != null">city = #{city},</if>
            <if test="signature != null">signature = #{signature},</if>
            <if test="email != null">email = #{email},</if>
            update_time = NOW()
        WHERE id = #{id} AND del_flag = 0
    </update>

    <insert id="insert" parameterType="com.smarttravel.user.domain.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (openid, nickname, avatar, gender, city, signature, status,
                          note_count, favorite_count, checkin_count, create_time, update_time, del_flag)
        VALUES (#{openid}, #{nickname}, #{avatar}, #{gender}, #{city}, #{signature}, COALESCE(#{status}, 1),
                COALESCE(#{noteCount}, 0), COALESCE(#{favoriteCount}, 0), COALESCE(#{checkinCount}, 0),
                NOW(), NOW(), COALESCE(#{delFlag}, 0))
    </insert>

    <select id="selectByOpenid" parameterType="string" resultMap="UserResult">
        SELECT <include refid="Base_Columns"/> FROM user
        WHERE openid = #{openid} AND del_flag = 0
        LIMIT 1
    </select>

    <!-- 批量物理删除用户 -->
    <delete id="batchDelete">
        DELETE FROM user
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>

